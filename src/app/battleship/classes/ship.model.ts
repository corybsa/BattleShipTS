import {Orientation} from './orientation.model';

export class Ship {
  private size: number;
  private health: number;
  private name: string;
  private orientation: Orientation;

  constructor() {}
}
